# DataBoard - Веб-админка для управления PostgreSQL

Современное веб-приложение для администрирования PostgreSQL баз данных с интуитивным интерфейсом и расширенными возможностями.

## Быстрый запуск

### Предварительные требования
- Docker
- Docker Compose
- 3000 порт свободен (можно изменить)

### Установка и запуск за 3 минуты

```bash
# 1. Скачиваем проект
git clone <репозиторий>
cd <папка-проекта>

# 2. Запускаем всё одной командой
docker-compose up --build

# 3. Открываем в браузере
# http://localhost:3000
```

> ⚠️ **Важно**: База данных при первом запуске уже содержит тестовые данные из файла `init.sql` (информация об агропредприятиях).

## 🌟 Основные возможности

### Панель управления
- Обзор всех таблиц базы данных
- Статистика по записям
- Быстрый доступ к таблицам
- Сводная информация о БД

### Редактор таблиц
- **Просмотр** данных с пагинацией
- **Добавление** новых записей
- **Изменение** существующих данных
- **Удаление** записей (с проверкой зависимостей)
- Экспорт таблиц в форматы:
  - 📁 Excel (.xlsx)
  - 📄 JSON (.json)

### SQL консоль
- Полноценный SQL-редактор
- Поддержка параметризованных запросов
- Примеры готовых запросов
- Сохранение результатов в Excel/JSON
- Подсказки по таблицам

### Утилиты
- **Резервное копирование** всей БД (формат .backup)
- **Восстановление** из резервной копии
- **Экспорт** выбранных таблиц
- **Архивирование** таблиц с удалением
- **Опасные операции** (требуют подтверждения):
  - Удаление отдельных таблиц
  - Полная очистка базы данных

## Структура проекта

```
.
├── app/                    # Код приложения
│   ├── main.py            # Основной сервер FastAPI
│   ├── database.py        # Модуль работы с PostgreSQL
│   ├── requirements.txt   # Python зависимости
│   └── templates/         # HTML шаблоны (4 страницы)
├── backups/              # Автоматически создаваемые бэкапы
├── exports/              # Экспортированные файлы
├── archives/             # Архивированные таблицы
├── docker-compose.yml    # Docker Compose конфигурация
├── Dockerfile           # Конфигурация Docker образа
├── init.sql             # Скрипт инициализации БД (тестовые данные)
└── .env.example         # Пример файла окружения
```

## Технологический стек

| Компонент | Технология |
|-----------|------------|
| **Backend** | Python 3.9 + FastAPI |
| **База данных** | PostgreSQL 15 |
| **Frontend** | HTML5 + CSS3 + JavaScript (Vanilla) |
| **Инфраструктура** | Docker + Docker Compose |
| **Дополнительно** | HTMX для динамики, FontAwesome для иконок |

## Начальная база данных

При первом запуске автоматически создаётся тестовая БД "AgroCultureDB" с таблицами:
- 🏢 Предприятия
- 🌾 Участки (поля)
- 🌱 Культуры
- 🌾 Посевы
- 📦 Уборка урожая
- 👨‍🌾 Сотрудники
- 🔄 Обработка полей

## Настройка окружения

Создайте файл `.env` в корне проекта (или используйте `.env.example`):

```env
# Настройки базы данных
DB_HOST=postgres
DB_PORT=5432
DB_NAME=AgroCultureDB
DB_USER=postgres
DB_PASSWORD=postgres

# Настройки приложения
APP_HOST=0.0.0.0
APP_PORT=3000
```

## Команды Docker

```bash
# Основные команды
docker-compose up              # Запуск
docker-compose up -d           # Запуск в фоне
docker-compose down           # Остановка
docker-compose logs -f        # Просмотр логов

# Полная перезагрузка
docker-compose down -v        # Остановка с удалением томов
docker-compose build --no-cache  # Пересборка без кэша
docker-compose up             # Запуск с чистой БД

# Проверка состояния
docker-compose ps            # Статус контейнеров
docker stats                # Потребление ресурсов
```

## Решение проблем

### Порт 3000 занят
Измените порт в `docker-compose.yml`:
```yaml
ports:
  - "3001:3000"  # Было 3000:3000
```
И обновите `.env`: `APP_PORT=3001`

### Ошибки подключения к БД
```bash
# Проверьте логи PostgreSQL
docker-compose logs postgres

# Перезапустите контейнеры
docker-compose restart
```

### Пустая база данных после восстановления
1. Убедитесь, что файл .backup корректен
2. Проверьте логи при восстановлении:
   ```bash
   docker-compose logs app | grep -i restore
   ```
3. Используйте файл `init.sql` для повторной инициализации

### Файлы .env не работают
Убедитесь, что у вас есть оба файла:
- `./.env` (используется Docker Compose)
- `./app/.env` (используется приложением внутри контейнера)

Значения в них должны быть одинаковыми.

## Важные особенности

### Безопасность
- Все деструктивные операции требуют подтверждения
- Удаление записей проверяет внешние ключи
- Критические действия требуют ввода подтверждающей фразы
- Восстановление БД полностью перезаписывает текущие данные

### Форматы файлов
- **Резервные копии**: PostgreSQL custom format (`.backup`)
- **Экспорт данных**: Excel (`.xlsx`) и JSON (`.json`)
- **Архивы**: комплект файлов (backup + Excel + JSON)

### Производительность
- Пагинация для больших таблиц (по 200 записей)
- Ограничение экспорта (до 50,000 записей на таблицу)
- Асинхронные операции для длительных задач

## Каталоги данных

Создаются автоматически при первом запуске:
- `backups/` - резервные копии БД
- `exports/` - экспортированные файлы
- `archives/` - архивы удалённых таблиц

> **Совет**: Эти каталоги монтируются в контейнеры, поэтому данные сохраняются при перезапуске.
